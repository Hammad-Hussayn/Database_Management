ALTER TABLE movie 
ADD COLUMN months TEXT, 
ADD COLUMN days INTEGER; 

#Task 2: we will be splitting the column release_dates to movie and days 

UPDATE movie 

SET months = SPLIT_PART(release_date, ' ', 1), 

    days = CAST(SPLIT_PART(release_date, ' ', 2) AS INTEGER);
